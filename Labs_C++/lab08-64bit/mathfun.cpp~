int main () {
  int x, y;
  cout << "Enter int x: ";
  cin >> x;
  cout << "Enter int y: ";
  cin >> y;
  cout << "product(x, y) = " << product(x, y) << endl;
  cout << "power(x, y) = " << power(x, y) << endl;
}
